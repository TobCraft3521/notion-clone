/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'vdouxtvoesezqxvbauso.supabase.co'
            }
        ],
    },
}

export default nextConfig
